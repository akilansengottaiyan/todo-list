# Task Definitions Directory

This directory contains auto-generated task definitions from JIRA tickets.

Files in this directory are generated by `npm run jira:task` and are gitignored by default.

To generate a task definition, run:

```bash
npm run jira:task -- YOUR-TICKET-KEY
```

Example:
```bash
npm run jira:task -- BP-401
```
